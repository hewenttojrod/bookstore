import FormBody from "@templates/form-body";

export default function BookstoreCatalog() {
  return (
    <FormBody title="Bookstore Catalog" subtitle="You are in Bookstore Catalog.">
      <p className="text-sm text-slate-600 dark:text-slate-300">
        Add table, filters, and form controls here. The container keeps full usable height.
      </p>
    </FormBody>
  );
}